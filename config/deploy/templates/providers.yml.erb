providers:
  twitter:
    key: "consumer_key"
    secret: "consumer_secret"
  facebook:
    key: "<%= facebook_app_id %>"
    secret: "<%= facebook_secret_key %>"
    options:
      client_options:
        ssl:
          ca_path: "/etc/ssl/certs"
