%h2 Create a Answer
= semantic_form_for [:admin, @answer], :html => { :multipart => true } do |form|
  = render :partial => 'form', :locals => { :form => form }
  = form.buttons do
    = form.commit_button 'Create'
    = form.commit_button 'Cancel', :button_html => {:class => 'follow_link', :href => admin_answers_path}
