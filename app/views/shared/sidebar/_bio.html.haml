-# @expects: local 'story'

.panel-1
  .panel-bar
    %h2= t('.posted_by', :fb_name => local_linked_profile_name(user)).html_safe
  .bio
    .profile-pic
      = local_linked_profile_pic user
    %p= raw((user.bio.present?) ? user.bio : default_bio(user)).fb_sanitize
