-# to do: needs way to hide if the user has already approved permissions

- cache :prompt_permissions_html do
  %div{:class => "panel-1 #{defined?(widget_page) ? widget_page.css_class : ''}"}
    .panel-bar
      %h2= t('.title')
    .panel_block
      .item-block
        %p= t('.details')
        %fb:prompt-permission{:perms => "email"}= t('.request')
