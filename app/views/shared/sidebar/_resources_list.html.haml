-# @expects: local @resources, @title

.panel-1
  .panel-bar
    %h2= title
  .item-list-wrap
    .item-list
      %ul
        - Resource.active.top(5).each do |resource|
          %li
            .meta-profile
              .profile-pic= local_linked_profile_pic resource.user
              %h5= link_to resource.title, resource_path(resource)
           
              
