#secondary-navigation
  %ul
    %li= link_to t('.browse_stories'), stories_path, :class => "tab #{sub_tab_selected?(@current_sub_tab, 'Browse Stories')}" 
    %li= link_to t('.browse_wires'), newswires_path, :class => "tab #{sub_tab_selected?(@current_sub_tab, 'Browse Wires')}" 
