#secondary-navigation
  %ul
    %li{:class => "tab #{sub_tab_selected?(@current_sub_tab, 'Browse Stories')}"}
      = link_to t('.browse_stories'), stories_path
    %li{:class => "tab #{sub_tab_selected?(@current_sub_tab, 'Browse Wires')}"}
      = link_to t('.browse_wires'), newswires_path
