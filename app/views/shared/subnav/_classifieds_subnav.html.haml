#secondary-navigation
  %ul
    %li{ :class => "tab #{sub_tab_selected?(@current_sub_tab, 'Browse')}"}
      = link_to t('classifieds.subnav.browse'), classifieds_path
    %li{:class => "tab #{sub_tab_selected?(@current_sub_tab, 'My Items')}"}
      = link_to t('classifieds.subnav.my_items'), my_items_classifieds_path
    %li{:class => "tab #{sub_tab_selected?(@current_sub_tab, 'New Item')}"}
      = link_to t('classifieds.subnav.post_item'), new_classified_path
